name: "CI"

on:
  push:
    branches:
      - main

jobs:
  show:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Show commit and files
        run: |
          echo "Commit: $GITHUB_SHA"
          echo "Actor: $GITHUB_ACTOR"
          ls -la
